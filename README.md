# Java-Groovy-Load-Balancer
A proof of concept Java load balancing system, which uses C++ code (through JNI) to monitor CPU usage, and move work between initalised nodes. Requires the Groovy library.
